"""Future LLM-based summarization over WorldMemory and EventLog.

Not implemented in the MVP. Narration is explicitly downstream and
read-only: it never runs inside the simulation loop and never influences
simulation state.
"""
