"""Simulation configuration.

Holds tick rate, the fixed per-tick system execution order, and feature
flags as data. Configuration here is read by
``living_diorama.simulation`` to decide how a run is wired together; it
contains no behavior of its own.
"""
