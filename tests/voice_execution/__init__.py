"""The Phase 29 voice execution test suite, collected as a package.

The ``__init__.py`` is load-bearing: without it the suite collects under
``python -m pytest`` and fails under the bare ``pytest`` console script CI
invokes -- the Phase 23 lesson, carried forward as structure rather than
memory.
"""
