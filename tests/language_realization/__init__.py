"""The Phase 26 language realization test suite.

Without this file the suite collects under ``python -m pytest`` and fails
under the bare ``pytest`` console script CI invokes -- the Phase 23 lesson,
kept.
"""
